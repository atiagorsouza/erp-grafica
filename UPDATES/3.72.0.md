---
versao: 3.72.0
data: 2026-08-27
commit: f89b059
tipo: minor
migracao_banco: nao
reseed: nunca
de: 3.71.0
portal: nao
---

# Boletim 3.72.0 — Comando de contencao do agente + auditoria geral + health enriquecido

## O que mudou

1. AGENTE-COMANDO.md: instrucao permanente do agente do servidor com 3 listas (verde/amarelo/vermelho), protocolo PARE-RELATE-ESPERE e formato de relato. Cole como system prompt do agente.
2. /api/health enriquecido: versao codigo x banco, BUILD_ID, QUAL banco o ERP le (sem credencial), latencia, uptime e avisos. Contrato antigo {ok:true} e status 200/500 preservados.
3. docs/AUDITORIA-2026-08-27.md: auditoria com o sistema RODANDO (nao por grep) - seguranca, concorrencia, fuso, integridade, precificacao, layout
4. Correcao de rumo: portal NAO e mais stub (feito na 3.69.0) e pricing.ts NAO deve ter server-only (e importado por 11 client components; adicionar quebra a build)

## O que você (agente do servidor) deve fazer

1. Leia AGENTE-COMANDO.md ANTES de qualquer coisa - ele define ate onde voce pode ir. Em conflito entre documentos, o mais restritivo vence.
2. Fluxo padrao do AGENTE-SERVIDOR.md secao 4 - nao pule etapas

## Como validar

1. curl -s http://127.0.0.1:3000/api/health deve trazer avisos: [] e versao.emDia: true
2. smoke 308 checks e /api/version upToDate true

## Migração de banco

**NÃO** — nenhuma alteração de schema nesta versão. O `update.sh`
roda mesmo assim (é idempotente) e o backup continua obrigatório.

## Portal do cliente (Hostinger)

Não afetado nesta versão — nada a fazer na Hostinger.

## Rollback

Se a versão não prestar (AGENTE-SERVIDOR.md §7):

```bash
BACKUP=$(cat .printflow/last-backup.path)
ls -lh "$BACKUP"                 # confirme que existe e tem tamanho
pm2 stop <nome-do-processo>
# banco não mudou — restaure só o código
mkdir -p /tmp/rollback && tar -xzf "$BACKUP/app-source.tgz" -C /tmp/rollback
rsync -a --delete --exclude '.env' --exclude 'node_modules' --exclude '.next' \
  /tmp/rollback/ /www/wwwroot/erp-grafica/
npm install && npm run build && pm2 restart <nome-do-processo>
```
