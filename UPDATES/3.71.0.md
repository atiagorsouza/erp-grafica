---
versao: 3.71.0
data: 2026-08-27
commit: b7b1002
tipo: minor
migracao_banco: nao
reseed: nunca
de: 3.70.1
portal: nao
---

# Boletim 3.71.0 — Entrega automatizada + lint zerado + conserto do SKU

## O que mudou

1. scripts/entregar.mjs: fecha versao, escreve o boletim, comita, faz push e imprime a ordem de servico pronta para o agente do servidor (npm run entregar)
2. SKU duplicado em produtos devolvia 500 com stack no log; agora responde 409 dizendo em QUAL produto o codigo ja esta (createProduct e updateProduct)
3. Lint de 15 problemas para 0: setState em effect (TopBar, MobileNav), aspas nao escapadas (Calendario, Produtos) e useMemo que bloqueava o React Compiler em PrintersEngine
4. next.config.ts: allowedDevOrigins para o preview em sandbox (so afeta next dev)
5. AGENTE-SERVIDOR.md 8.1/8.2 (portal Hostinger passo a passo) e 9.1/9.2 (de onde vem a mensagem)
6. COMO-ENTREGAR.md: guia de uma pagina para o dono

## O que você (agente do servidor) deve fazer

1. Fluxo padrão do AGENTE-SERVIDOR.md §4 — não pule etapas:

```bash
cd /www/wwwroot/erp-grafica
git status                      # tem que estar limpo
git pull origin main
bash scripts/update.sh          # backup → deps → schema → build
pm2 restart <nome-do-processo>  # confira com: pm2 ls
```

## Como validar

1. npm run lint deve sair LIMPO (baseline agora e 0, nao mais 11)
2. smoke deve dar 308 checks e terminar com sucesso

## Migração de banco

**NÃO** — nenhuma alteração de schema nesta versão. O `update.sh`
roda mesmo assim (é idempotente) e o backup continua obrigatório.

## Portal do cliente (Hostinger)

Não afetado nesta versão — nada a fazer na Hostinger.

## Rollback

Se a versão não prestar (AGENTE-SERVIDOR.md §7):

```bash
BACKUP=$(cat .printflow/last-backup.path)
ls -lh "$BACKUP"                 # confirme que existe e tem tamanho
pm2 stop <nome-do-processo>
# banco não mudou — restaure só o código
mkdir -p /tmp/rollback && tar -xzf "$BACKUP/app-source.tgz" -C /tmp/rollback
rsync -a --delete --exclude '.env' --exclude 'node_modules' --exclude '.next' \
  /tmp/rollback/ /www/wwwroot/erp-grafica/
npm install && npm run build && pm2 restart <nome-do-processo>
```
